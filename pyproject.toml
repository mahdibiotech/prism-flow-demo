[project]
name = "prism-flow-demo"
version = "0.1.0"
description = "Reproducible omics workflow and provenance demonstrator"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100
